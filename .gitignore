# Exclude certain plugin data (don't expose sensitive data, keep personal settings intact)
**/cursor-positions.json

# Exclude all files needed to test plugins
Tests/

# Exclude workspace cache (for desktop and mobile)
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Exclude OS settings and caches
**/desktop.ini
.DS_Store
